# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Env & secrets (never commit — share via your team channel)
.env
.env.*
!.env.example
deploy/cloudrun-env.yaml

# Firebase / GCP service accounts (never commit)
*credentials*.json
*firebase-adminsdk*.json

# Firebase local build output
.firebase/

# Node / Next
node_modules/
.next/
out/
dist/

# OS / IDE
.DS_Store
Thumbs.db
*.pem
